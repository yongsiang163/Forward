{
  "name": "forward-functions",
  "description": "Cloud Functions for Forward — Gemini API proxy so user keys never live client-side.",
  "private": true,
  "type": "commonjs",
  "engines": { "node": "20" },
  "main": "index.js",
  "dependencies": {
    "firebase-admin": "^12.0.0",
    "firebase-functions": "^5.0.0"
  }
}
